# Planning sesi 2026-09-09

Hanya berisi yang dibicarakan di sesi ini. D-021 … D-035 disetujui dan tercatat di `docs/DECISIONS.md`.

---

### P-LANG-FLICKER — Layar berkedip gelap sebentar saat ganti bahasa
- **Laporan owner (2026-09-09)**: saat mengganti bahasa, layar berkedip gelap sebentar.
- **Audit (terverifikasi di kode)**: bukan bug logika — tap bahasa memanggil `SettingsViewModel.onLanguageSelected()` (`SettingsViewModel.kt:295-299`) yang mem-cerminkan pilihan ke `LocaleManager.setApplicationLocales()` di API 33+ (D-022); sistem me-recreate Activity (perilaku platform), dan `AppRoot` mulai lagi dari surface `Loading` hitam (`AppRoot.kt:58-72`) sampai flag onboarding terbaca. String tetap benar; yang terlihat hanya bingkai kosong + jeda recreate. Di bawah API 33 tidak ada recreate (murni recompose, tanpa kedip).
- **D-028 (DISETUJUI owner 2026-09-09, tercatat di `docs/DECISIONS.md`)**: "`Theme.QuakeAlert` memakai `android:windowBackground` `@color/app_window_background` (sama dengan `BackgroundGradientBottom`) sehingga jeda recreate terbaca sebagai still frame gelap, bukan flash kontras." Recreate tetap terjadi (syarat mirror sistem); murni presentasi, tanpa perubahan semantik bahasa/kontrak/delivery. Verifikasi: switch EN↔ID jeda gelap menyatu; cold-start normal; suite unit + lint hijau.

---

### P-ONBOARD-FULLSCREEN — Pill fullscreen tidak langsung update setelah izin
- **Laporan owner (2026-09-09, screenshot)**: habis mengizinkan, halaman fullscreen masih "not allowed"; harus pindah halaman dulu baru jadi "Allowed". Halaman izin lain langsung update.
- **Audit Hermes 2026-09-09 (terverifikasi di kode)**:
  1. Halaman baterai selalu lewat `settingsLauncher` (`openBatteryOptimizationSettings` → `launch(...)`) sehingga callback refresh (`OnboardingScreen.kt:164-169`) selalu fire saat kembali.
  2. Halaman fullscreen di API 34+ membuka halaman khusus via `startActivity` langsung (`FullscreenIntentSettings.kt:31`), melewati launcher — callback tidak pernah fire, `fullscreenGranted` (`remember`, `OnboardingScreen.kt:138`) basi sampai ada recomposition lain (geser halaman).
  3. Koreksi teori audit sebelumnya: judul statis vs pill reaktif benar adanya tetapi BUKAN bug yang dilaporkan; itu catatan lanjutan, bukan D-026.
- **D-026 (DISETUJUI owner 2026-09-09, tercatat di `docs/DECISIONS.md`)**: "Halaman khusus fullscreen dibuka via `settingsLauncher` (tambah varian penerima launcher di `FullscreenIntentSettings.kt`) sehingga callback refresh fire seperti halaman baterai." Tanpa string baru. Verifikasi: izinkan → pill langsung "Allowed" tanpa pindah halaman; tolak → tetap ajakan; suite unit + lint hijau.

### P-CHECKLIST-ICON — Icon lokasi Delivery Checklist beda dari onboarding
- **Laporan owner (2026-09-09, screenshot)**: baris Precise Location memakai pin (`ic_pin_location`, `SettingsComponents.kt:393`), sedangkan halaman onboarding memakai `ic_location_permission`. Tiga baris lain (notifikasi, baterai, fullscreen) sudah sama dengan onboarding.
- **D-027 (DISETUJUI owner 2026-09-09, tercatat di `docs/DECISIONS.md`)**: "Baris Precise Location memakai `ic_location_permission`, konsisten dengan onboarding." Satu drawable, tanpa string baru. Verifikasi: visual checklist vs onboarding; suite unit + lint hijau.

### P-NOTIF-ICON — Icon bar notifikasi terlalu kecil
- **Laporan owner (2026-09-09)**: icon di notification bar terlihat terlalu kecil.
- **Audit (terverifikasi di kode)**: ketiga notifier (`WarningNotifier.kt`, `StatusNotifier.kt`, `UpdatesNotifier.kt`) memakai `ic_quake_logo` langsung sebagai small icon; drawable itu didesain untuk launcher (motif 65% viewport + margin safe-zone adaptive icon). Status bar merender ~24dp lewat alpha mask sehingga margin terbaca sebagai icon mungil. Bukan bug gambar — konsekuensi D-023 (small icon = varian utuh).
- **D-029 (DISETUJUI owner 2026-09-09, tercatat di `docs/DECISIONS.md`)**: "Small icon memakai `ic_notification_small` — motif dan stroke sama persis (path verbatim), dibungkus grup skala 1.35 sehingga mengisi viewport; pengecualian optik tercatat atas D-023, launcher/About/onboarding tetap varian utuh." Verifikasi: visual shade di ukuran status bar; suite unit + lint hijau.

### P-ALERT-LANG — Full-screen alert tidak mengikuti bahasa (selalu Inggris)
- **Laporan owner (2026-09-09)**: di halaman test warning, full-screen alert selalu Inggris.
- **Audit (terverifikasi di kode)**: `WarningActivity` memanggil `ActiveAlertCard` tanpa `lang` (default EN, `WarningAlertComponents.kt:82`) dan tidak pernah membaca bahasa tersimpan — sementara `WarningScreen.kt:198-202` meneruskan `lang` dengan benar dan shade notifikasi sudah lokal. Jadi **semua** full-screen alert (test maupun asli) selalu Inggris, bukan cuma halaman test.
- **D-030 (DISETUJUI owner 2026-09-09, tercatat di `docs/DECISIONS.md`)**: "`WarningNotifier` membekukan tag bahasa yang sudah di-resolve sebagai `EXTRA_LANG`; Activity resolve sekali (`?: EN`) di `onCreate`/`onNewIntent` dan teruskan ke kartu — snapshot koheren, nol I/O di jalur darurat." Verifikasi: drill ID → kartu Indonesia penuh; drill EN → Inggris; suite unit + lint hijau.

### P-FILTER-BUTTONS — Tombol Reset dan Apply sama-sama merah
- **Laporan owner (2026-09-09)**: di halaman filtering, tombol Reset dan Apply sama-sama warna merah; sebaiknya dibedakan agar user-friendly. Pilihan owner: Apply teal, Reset ghost.
- **Audit (terverifikasi di kode)**: keduanya memakai `FilterDialogAction` yang sama (`QuakeFilterDialog.kt`) dengan chrome identik `EmergencyCtaFill` (wine-red) — bukan cuma hue sama, bobot visual pun sama sehingga aksi utama tak terbedakan.
- **D-031 (DISETUJUI owner 2026-09-09, tercatat di `docs/DECISIONS.md`)**: "Apply solid `FilterActiveFill` (`#003346`); Reset ghost (transparan + stroke yang sama, geometri identik); merah wine dicadangkan untuk konteks darurat." Nol token warna baru, tanpa string baru. Verifikasi: screenshot dialog; suite unit + lint hijau.

### P-GITHUB-LINKS — Link GitHub mengarah ke repo yang benar
- **Permintaan owner (2026-09-09)**: link GitHub mengarah ke `https://github.com/banana-pixel/QuakeAlert`, sinkron di halaman About dan onboarding.
- **Audit (terverifikasi di kode)**: URL repo hidup sebagai konstanta privat terpisah di `AboutModal.kt` (`AboutLinks.GITHUB_PAGES`, masih `.../QuakeAlert-App/`) dan `ReadyText.kt` (`GITHUB_REPO_URL`, masih `.../QuakeAlert-App`) — duplikasi inilah yang membuat kedua halaman meleset diam-diam. `PROFILE_URL`, `DONATE`, dan teks link terlihat sudah benar.
- **D-032 (DISETUJUI owner 2026-09-09, tercatat di `docs/DECISIONS.md`)**: "`ProjectLinks` di root package satu-satunya definisi repo/profil/Pages (Pages ikut repo baru, pilihan owner); `AboutLinks` hanya menyimpan contact/support; tanpa alias penerus." Tanpa string baru. Verifikasi: tap manual ketiga link; suite unit + lint hijau.

### P-SEA-DOTS — Titik event di peta kartu Recent Seismic Activity
- **Temuan** (laporan owner, 2026-09-09, screenshot Warning → SEE DETAILS): peta mini di kartu "Recent Seismic Activity" hanya basemap gelap + label kota (Bogor, Bandung, dst), tanpa titik. Kartu menyebut "6 events" tetapi peta tidak menunjukkan di mana event itu terjadi.
- **Status kini (terverifikasi di kode)**:
  - `RecentSeismicActivity` (`ui/warning/WarningUiState.kt:109`) hanya membawa agregat: `eventCount`, `mostRecent`, `strongest`, + lat/lon perangkat. Tidak ada koordinat per event.
  - `fetchRecentActivity` (`ui/warning/WarningViewModel.kt:363`) sudah memegang `events` dari `fetchEvents` (radius 200 km, 30 hari) tetapi membuang koordinat; hanya agregat yang disimpan.
  - `RecentSeismicActivityCard` (`ui/warning/WarningComponents.kt:491`) memanggil `QuakeMap` tanpa `markers` (komentar: "this card draws no overlays of its own").
  - `QuakeMap` (`ui/common/QuakeMap.kt:159`) sudah mendukung `markers: List<MapMarker>` (MapLibre circle layer) — dipakai kartu lain, tinggal diumpan.
- **Arah rencana (belum audit, belum ada nomor D)**: tambah daftar titik per event (lat/lon + intensitas, dibatasi N + penanda cap) ke model; teruskan sebagai `markers` ke `QuakeMap`; gaya dot mengikuti skala intensitas yang sudah ada; kasus 0 event / offline / tanpa posisi tetap tanpa titik palsu (prinsip kartu ini: tidak menemukan angka).
- **Audit Hermes 2026-09-09 (terverifikasi di kode)**:
  1. Data sudah di tangan: `EarthquakeEvent` membawa lat/lon centroid + `pgaGal`/`mmi`/`intensityLabel`; `fetchRecentActivity` memegang ≤100 event (`ACTIVITY_PAGE_LIMIT=100`) — tanpa request baru, tinggal teruskan.
  2. `MapMarkerKind` hanya mengenal STATION_OFFLINE/ONLINE, SELECTED, USER — perlu kind baru EVENT dengan warna beda dari station agar tidak tertukar. Satu warna dulu (bukan gradasi intensitas) supaya kecil dan mudah direview.
  3. Kejujuran cap: dots = tepat events yang mendasari count (satu halaman). Jika penuh, count sudah bertulis "100+" sehingga "+"-nya mencakup keterbatasan dots — tanpa string baru. Tiap dot = event tercatat asli, tidak ada yang difabrikasi.
  4. Kasus 0 event/offline/tanpa posisi: tanpa dots (tidak ada yang bisa diplot jujur); perilaku honest-empty existing tidak berubah. Dots statis per bukaan kartu, sama seperti statistiknya.
  5. Preseden lapisan: `SensorsUiState:217` sudah membangun `MapMarker` di UiState — `RecentSeismicActivity.dots: List<MapMarker>` dapat diterima. Tidak seperti D-021/D-022, mapping ini murni → unit-testable JVM.
- **D-024 (DISETUJUI owner 2026-09-09, tercatat di `docs/DECISIONS.md`)**: "RecentSeismicActivity membawa events yang diplot; kartu menggambar satu dot per counted event (kind EVENT, warna beda dari station)." Verifikasi: unit test mapping + cap; cek visual (6 event → 6 dot di radius; 0 event → peta bersih); suite unit + lint hijau.
- **Syarat lanjut**: implementasi oleh OpenCode.

### P-NOTIF-TOGGLE — Toggle status notification off tidak menghapus notifikasi
- **Laporan owner (2026-09-09)**: "Show status on notification shade" dimatikan di Settings tetapi notifikasi persistent tetap tampil.
- **Audit Hermes 2026-09-09 (terverifikasi di kode)**:
  1. `StatusNotifier.clear()` ada (`StatusNotifier.kt:171-174`) tetapi TIDAK PERNAH dipanggil siapa pun — satu-satunya pemanggil `notify()` adalah `AppViewModel:192`.
  2. Collector `observeStatusNotification` menggabungkan toggle (`repository.statusNotification`) ke `prefs.enabled` tetapi tidak pernah membacanya — selalu `notify()`. Mematikan toggle hanya menyimpan preferensi.
  3. Lebih buruk: `distinctUntilChanged()` pada pasangan (status, lang) — toggle off tidak mengubah pasangan itu sehingga collector bahkan tidak fire ulang. Notifikasi basi bertahan selamanya (ongoing + silent).
  4. Gejala turunan: restart aplikasi mem-posting ulang notifikasi meski toggle off (collector selalu `notify()` saat start). Dengan perbaikan, restart justru self-healing (clear saat disabled).
  5. Cakupan: tidak ada `BOOT_RECEIVER`/pemanggil lain — satu-satunya jalur post adalah collector ini. Notifikasi transient (`WarningNotifier`, `UpdatesNotifier`) tidak terkait.
- **D-025 (DISETUJUI owner 2026-09-09)**: "Toggle off menghapus notifikasi: collector meng-emit triple (status, lang, enabled); enabled → `notify()`, off → `clear()`." Toggle on tanpa OS grant tetap ter-suppress oleh `canPost()` (pesan Settings sudah mencakupnya). Verifikasi: off → hilang instan; on → muncul; restart saat off → tetap hilang; suite unit + lint hijau.

### P-NOTIF-SYNC — Status "blocked" basi setelah izin diberikan
- **Laporan owner (2026-09-09, screenshot)**: notifikasi status "Alerts blocked by system settings" tetap tampil meski izin notifikasi sudah diberikan.
- **Diagnosis (terverifikasi di kode)**:
  1. Snapshot basi (primer): `systemState` hanya di-refresh di `onAppForegrounded()` yang dipicu `LifecycleStartEffect` = ON_START (`AppRoot.kt:50`). Dialog izin sistem hanya mem-pause activity (tanpa stop), jadi grant via Settings in-app tidak memicu ON_START — snapshot `notificationsPermitted=false` bertahan. Emisi collector berikutnya (sync timestamp, bahasa, toggle) mem-posting ulang notifikasi "blocked" padahal izin sudah ada (`AppViewModel.kt:125-133, 168-194`).
  2. Cek sempit (sekunder): `canPostNotifications()` (`device/DeliveryPrerequisites.kt:30`) hanya cek runtime permission `POST_NOTIFICATIONS` — tidak mendeteksi toggle app-level dimatikan (`areNotificationsEnabled`) maupun channel di-set NONE. Arah berlawanan (lapor "aktif" padahal tak bisa masuk) tapi satu area yang sama.
- **Audit Hermes 2026-09-09 (terverifikasi di kode)**:
  1. Dua pembaca, dua kadensi: Settings UI refresh tiap ON_RESUME (`SettingsScreen.kt:117` + `refreshSystemState()`) sehingga selalu benar; notifikasi status hanya ON_START (`AppRoot.kt:50` `LifecycleStartEffect` → `AppViewModel.onAppForegrounded`). Dialog izin runtime tidak memicu ON_START → snapshot basi `false` + emisi collector berikutnya mem-posting "blocked" meski izin sudah ada. Cocok dengan gejala.
  2. `canPostNotifications()` (`DeliveryPrerequisites.kt:30`) buta terhadap toggle app-level dan channel NONE. Blast radius perubahan semantik: `AlertRaiser:133` (gate local-test), `AppViewModel` x2, `SettingsViewModel` x3, `WarningViewModel:108`.
  3. Preseden `AlertPresentationHealth`: degradasi channel-level bersifat diagnostik, tidak pernah jadi gate — perbaikan tidak boleh melipat importance channel ke dalam gate.
  4. Test: hanya JVM murni (copy + toggle flow); `DeliveryPrerequisites` butuh Context → tidak ada permukaan baru yang unit-testable, verifikasi manual wajib.
- **D-021 (DISETUJUI owner 2026-09-09, tercatat di `docs/DECISIONS.md`)**: "Notifikasi status membaca ulang grant OS di setiap resume, dan 'boleh posting' berarti runtime grant + toggle app-level." Isi: (F1) `LifecycleResumeEffect` di `AppRoot` → refresh `systemState` (syncIfStale tetap terjaga guard-nya); (F2) `canPostNotifications()` → `NotificationManagerCompat.areNotificationsEnabled()` + update KDoc; channel tetap diagnostik. Verifikasi: grant via dialog in-app → status flip tanpa backgrounding; toggle app-level off/on di system settings → flip blocked/active; suite unit + lint hijau.
- **Syarat lanjut**: implementasi oleh OpenCode.

### P-LANG-SYSTEM — Bahasa aplikasi mengikuti bahasa HP
- **Keputusan owner (2026-09-09)**: bahasa aplikasi default mengikuti bahasa HP — HP Indonesia → Indonesia, selain itu → Inggris. Pilihan eksplisit user di Settings tetap menang atas default sistem.
- **Audit Hermes 2026-09-09 (terverifikasi di kode)**:
  1. Resolver tidak perlu diubah: `resolveDisplayLanguage(null)` sudah ikut `Locale.getDefault()` dan sudah diuji (`DisplayLanguageTest`: null/empty/unknown → sistem, EN terakhir). Yang mematikan cabang sistem hanya default `"en"` repository.
  2. Permukaan perubahan mekanis: `AppSettingsRepository.language` → `Flow<String?>` (null = System, hapus `DEFAULT_LANGUAGE`); ~10 call-site `.map { resolveDisplayLanguage(it) }` sudah menerima nullable (nol logika berubah); one-shot `first()` di service sudah toleran null. `AppLanguage` + entri SYSTEM (tag null); `fromTag(String?)`: null → SYSTEM, unknown → EN (tetap).
  3. Tanpa AppCompat di proyek (hanya core-ktx): cermin pilihan eksplisit ke system settings HP via framework `LocaleManager.setApplicationLocales()` (API 33+, version-guard); <33 in-app saja. Tanpa dependensi baru.
  4. Background/service (`AlertRaiser`, `QuakeMessagingService`, `BackgroundAlertBridge`) sudah resolve via `resolveDisplayLanguage(stored)` → ikut otomatis, tanpa campuran bahasa.
  5. Migrasi: instalasi lama tersimpan "en" = eksplisit EN (pra-rilis, diterima). Segmented Settings 2→3 opsi + label SYSTEM ("System", fixed seperti "EN"/"ID").
- **D-022 (DISETUJUI owner 2026-09-09, tercatat di `docs/DECISIONS.md`)**: "Bahasa default = System (null tersimpan); HP Indonesia → ID, lainnya EN; pilihan eksplisit menang dan dicerminkan ke `LocaleManager` di API 33+." Verifikasi: locale ID → ID otomatis; locale lain → EN; ganti bahasa HP → ikut setelah recreate; pilihan eksplisit tidak tertimpa; suite unit + lint hijau.
- **Syarat lanjut**: implementasi oleh OpenCode.

### P-ICON-SVG — Logo resmi sebagai icon + elemen aplikasi
- **Keputusan owner (2026-09-09)**: pasang logo QuakeAlert (rounded) di modal About ganti `AboutLogoBadge`; onboarding menyusul. Logo dipakai sesuai SVG — bentuk/motif tidak boleh diubah; adjustment minor (sizing, stroke size) diizinkan (small icon notifikasi = varian transparan utuh).
- **Referensi**: `docs/brand/` — `quakealerticon.svg` (merah, opaque) + `quakealerticontr.svg` (transparan), keduanya verbatim + `README.md` catatan konversi.
- **Audit Hermes 2026-09-09 (terverifikasi)**:
  1. Varian transparan hanya berisi stroke (putih 30px, 10 path + 1 rect, viewBox 512) tanpa gradien/filter/teks/mask — semua fitur terdukung VectorDrawable. 5 transform grup (1 matrix-scale, 2 translate, 2 rotate) terpetakan ke `<group>` atau di-flatten saat konversi.
  2. Launcher: background = rect gradien merah (`#2c0c23`→`#82283a` diagonal, dari varian merah) sebagai layer sendiri; foreground = varian transparan; monochrome = varian transparan langsung (satu warna, sistem yang tint). WebP `mipmap-*` (API<26) diekspor ulang dari layer baru. Placeholder robot/grid hijau terhapus total.
  3. About (`AboutLogoBadge`, `AboutModal.kt:190`): ganti 3 disc + `ic_recording_wave` dengan `Image` drawable logo + `clip(RoundedCornerShape)`. Onboarding (`OnboardingScreen.kt:491`): `ic_puzzle_piece` → logo. Notifikasi (`StatusNotifier`, `UpdatesNotifier`, `WarningNotifier` small icon): varian transparan utuh sesuai keputusan owner.
  4. Batasan owner dipatuhi: tidak ada perubahan bentuk/motif; hanya sizing/stroke bila perlu.
- **D-023 (DISETUJUI owner 2026-09-09, tercatat di `docs/DECISIONS.md`)**: "Logo resmi (varian transparan, bentuk utuh) menggantikan placeholder di launcher (foreground/background/monochrome + WebP), About, onboarding, dan small icon notifikasi." Verifikasi: cek visual di perangkat (launcher mask bulat/squircle, About rounded, onboarding, shade notifikasi), suite unit + lint hijau.

### P-TESTDRIVE-FOLLOWUP — Koreksi dari test drive owner (build D-029…D-032)
- **Hasil owner (2026-09-09)**: semua PASS kecuali link GitHub di About (membuka github.io yang rusak — seharusnya sama dengan onboarding), plus dua keputusan: Donate mengarah ke `https://saweria.co/bananapixel`, dan Apply filter lebih baik hijau (dipilih hijau wizard).
- **D-033 (DISETUJUI owner 2026-09-09, tercatat di `docs/DECISIONS.md`)**: "About GitHub membuka `REPO_URL` dengan label `GitHub` (proper noun, tanpa string baru); `PAGES_URL` yang tak terpakai dihapus; Donate ke Saweria; Apply memakai `WizardConfirmActionFill` sebagai amendemen tercatat D-031." Verifikasi: tap manual ketiga link + screenshot dialog filter; suite unit + lint hijau.
- **Test drive putaran 2 (2026-09-09): 3/3 PASS** — About → repo, Donate → Saweria, Apply hijau olive. Tercatatkan di `docs/CURRENT_STATE.md` § Demonstrated bersama putaran 1.

### P-LAUNCHER-ICON — Icon launcher rakitan Asset Studio
- **Konteks (2026-09-09)**: logo launcher D-023 di-revert (`5ece266`, owner merakit ulang via Asset Studio), lalu dimuat sebagai icon kustom (`7ddf48a`: layer adaptif, WebP ulang, `anydpi-v26`, icon Play Store). Bentuk/motif tetap milik D-023; yang dicatat di sini hanya mekanikanya.
- **D-034 (DISETUJUI owner 2026-09-09, tercatat di `docs/DECISIONS.md`)**. Verifikasi: test drive putaran 1 PASS (terpasang berdampingan via id `.debug`); suite unit + lint hijau.

### P-ONBOARD-GLOBE — Globe di samping chip onboarding halaman 2
- **Konteks (2026-09-09)**: `ic_globe_05` di samping chip sensor (Figma 1:341, `e968fa6`). Murni presentasi.
- **D-035 (DISETUJUI owner 2026-09-09, tercatat di `docs/DECISIONS.md`)**. Verifikasi: test drive putaran 1 PASS (visual); suite unit + lint hijau.

### P-SETUP-INVENTORY — Inventaris kredensial non-rahasia
- **Konteks (2026-09-09)**: keystore, `google-services` debug/rilis, dan kunci FCM server semuanya sudah ada dan benar tempatnya, tapi tak tercatat di mana pun — mesin ganti berarti mulai dari nol.
- **Dicatat di `docs/FIREBASE_SETUP.md` §5**: tabel nama + lokasi saja (tanpa nilai), terverifikasi 2026-09-09.

### P-SWIPE-ANDROID14 — Swipe notifikasi ongoing sejak Android 14
- **Temuan (2026-09-09, dokumentasi resmi)**: `setOngoing(true)` tetap cara yang benar; swipe-dismiss di API 34+ adalah perilaku platform (developer.android.com, behavior-changes-all), bukan bug dan bukan penyimpangan ROM. Keputusan owner: tidak ada perubahan kode.
- **Dicatat satu baris di KDoc `StatusNotifier`** agar tidak "diperbaiki" keliru di masa depan.

### P-FCM-SERVER — Kunci FCM server terverifikasi aktif (VALIDATED, rilis ditunda)
- **Pemeriksaan owner-machine → VPS (2026-09-09, read-only via SSH)**: `.env.prod` memuat `FCM_PROJECT_ID` / `FCM_CREDENTIALS_FILE` terisi, `deploy/secrets/fcm-service-account.json` ada (600, 2380 B), dan log server produksi mencatat `FCM sender aktif` saat start 2026-09-07 (`project_id: quakealert26`). Nilai secret tidak pernah ditampilkan.
- **Batas klaim**: yang VALIDATED hanya inisialisasi sender di server — delivery push belum pernah terjadi di produksi dan tetap di *NOT demonstrated*. Tercatat di `docs/CURRENT_STATE.md` § Demonstrated.
- **Keputusan owner**: rilis urusan nanti; tidak ada build rilis dalam paket ini.
